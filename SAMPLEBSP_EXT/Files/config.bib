MEMORY
	NK				8C200000  02000000  RAMIMAGE
	RAM				8E200000  01800000  RAM

CONFIG
    COMPRESSION=ON
    KERNELFIXUPS=ON

IF IMGTRUSTROMONLY
    IF IMGNOTALLKMODE
       ROMFLAGS=12
    ELSE
       ROMFLAGS=10
    ENDIF
ELSE
    IF IMGNOTALLKMODE
       ROMFLAGS=02
    ELSE
       ROMFLAGS=00
    ENDIF
ENDIF
	ROMSTART=8C200000   
	ROMWIDTH=32
	ROMSIZE=02000000

